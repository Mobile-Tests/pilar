name: dashboard
description: A Pilar Mobile Test dashboard package.
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.4.4 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  cached_network_image: ^3.3.1
  core: ^0.0.1
  design_system: ^0.0.1
  flutter:
    sdk: flutter
  i18n: ^0.0.1
  infinite_scroll_pagination: ^4.0.0
  network: ^0.0.1
  percent_indicator: ^4.2.3

dev_dependencies:
  build_runner: ^2.4.11
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4

flutter:
  uses-material-design: true
